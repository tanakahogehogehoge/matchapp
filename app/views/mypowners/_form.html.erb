<section class="function container-fluid">
  <div class="row">
    <div class="col-xs-offset-2 col-xs-8 col-xs-offset-2 col-sm-offset-3 col-sm-6 col-sm-offset-3">
      <%= form_for(@mypowner, url: choose_new_or_edit) do |f| %>
        <% if @mypowner.errors.any? %>
          <div id="error_explanation">
            <ul>
              <% @mypowner.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <h3>氏名</h3>
        <%= f.text_area :owner_name, :size=>"50x1" , class: 'form-control', placeholder: "フルネームで記載、ふりがなは不要" %>
        <h3>住所</h3>
        <%= f.text_area :owner_address, :size=>"50x5", class: 'form-control', placeholder: "マンション等の場合は部屋番号まで記載" %>
        <br>
        <h3>登録銀行口座</h3>
        <%= f.text_area :owner_bank_account, :size=>"50x1", class: 'form-control', placeholder: "ハイフン不要、銀行番号(4桁)支店番号（３桁）口座番号（７桁ないし８桁）の順に記入、例：00011230123456" %>
        <br>
        <br>
        <%= f.submit '登録', class: 'btn btn-danger btn-block' %>
      <% end %>
    </div>
  </div>
</section>
